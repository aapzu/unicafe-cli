
const assert = require('assert')
const proxyquire = require('proxyquire')

describe('menuData', () => {
    let menuData
    beforeEach(() => {
        menuData = require('../../data/menu-data')
    })
    describe('getMenuByUserQuery', () => {

    })
})
